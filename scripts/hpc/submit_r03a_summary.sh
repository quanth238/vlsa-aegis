#!/usr/bin/env bash
set -euo pipefail

echo "standalone R03A summary submission is retired" >&2
echo "submit_r03a_grouped_array.sh registers the dual afterany verifier before releasing either source array" >&2
exit 2
