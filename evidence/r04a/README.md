# R04A allocation-backed label-contract evidence

R04A is a one-case, no-learning apparatus smoke. It proves that a real eager
sampler trace, the unchanged inverse-transformed physical action, duplicate
five-action simulator replays, and the resulting continuation-clearance label
are exactly bound. It is not a probe-training, prediction, guidance, efficacy,
novelty, or transport result.

The raw 9.97 MB artifact remains on allocation-visible storage:

```text
/mnt/data/quanth/experiments/crfs-oracle/
  r04a-label-contract-smoke-20260714a/
  crfs-93365b8b851365f2/r04-label-contract.json
```

Its SHA-256 is
`b820793ec42a5228c858e297d13806d8ae7f02e5cc7a765769316473d795f285`.
The compact checked-in record is `r04a-validation.json`.

- Source Slurm array task: `27514_0`, completed `0:0` on
  `worker-mig-3g40gb-0`.
- Independent validator: Slurm job `27516`, completed `0:0` on `worker-2` with
  zero errors.
- Descriptive inspection: Slurm job `27518`; it did not change or promote the
  artifact.
- Reviewed source commit: `1138be311e255d891e8a3dfaace1a4e631da7479`, clean.
- Frozen config SHA-256:
  `561128a5a05710e50b282582463127ee3f8cd87c9ebbaee822f8c4602fda1c25`.
- Canonical config hash recorded inside the raw artifact:
  `95cb28780dc029e805eca619bef9d6a1a655505fc050eaeee21b4d535f7cc368`.
- Five trace-step records contain two sampler calls each, for ten total calls
  and five exact duplicate pairs.

The reused case is permanently restricted to
`apparatus_only_never_train_calibrate_validate_test_or_claim`.
